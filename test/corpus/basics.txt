==================
Simple curl block
==================

curl https://example.com
h1
  a

---

(source_file
  (block
    (directive
      (typeof)
      (url))
    (query
      (cssquery))
    (query
      (indentation)
      (cssquery))))

==================
Comment
==================

// This is a comment
curl https://example.com

---

(source_file
  (comment)
  (block
    (directive
      (typeof)
      (url))))

==================
Next page selector
==================

curl https://example.com
.title
> a.morelink

---

(source_file
  (block
    (directive
      (typeof)
      (url))
    (query
      (cssquery))
    (nextpage
      (cssx))))

==================
Pipe operator
==================

curl https://example.com
h1 | wc -c

---

(source_file
  (block
    (directive
      (typeof)
      (url))
    (query
      (cssquery)
      (pipe))))
